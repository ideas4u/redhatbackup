# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
	. ~/.bashrc
fi

# User specific environment and startup programs

PATH=$PATH:$HOME/bin

export PATH

echo "From /root/.bash_profile"
echo "Welcome to my zone."
echo "Make progress 1% everyday.:)"
